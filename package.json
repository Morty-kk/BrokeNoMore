{
  "name": "brokennomore",
  "version": "1.0.0",
  "description": "Static frontend with Prisma API routes for Vercel",
  "scripts": {
    "dev": "vercel dev",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "build": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "prisma": "^5.14.0",
    "vercel": "^33.2.0"
  }
}
