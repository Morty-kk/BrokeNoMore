{
  "name": "brokennomore",
  "version": "1.0.0",
  "description": "Static frontend with Express + Prisma backend",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",

    // nur fürs lokale Entwickeln:
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",

    // 👇 das ist wichtig für Vercel
    "build": "npm run prisma:generate"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "@prisma/extension-accelerate": "^2.0.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^6.18.0"
  }
}
